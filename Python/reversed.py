# This is very similar to range, I will use the same example below

max_num = int(input('The range is '))
# use reversed to print the numbers backwards
for n in reversed(range(max_num)):
    print(n)
    
# This will print the number input by the user but in reverse
    
